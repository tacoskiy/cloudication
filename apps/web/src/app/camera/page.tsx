import { CameraView } from "@/features/camera";

const CameraPage = () => {
  return <section className="w-full h-fit flex justify-center items-center px-3">
    <CameraView />
  </section>;
};

export default CameraPage;